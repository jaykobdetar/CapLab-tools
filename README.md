# CapLab-tools
Various tools for Capitalism Lab that I personally find useful, but that don't make sense to create their own repo
Map Viewer - Extracts the map from a save file and saves it as a JSON file, which can be loaded with map_viewer.html to view and verify the data is correct. 
